# -*- coding: utf-8 -*-
'''
Created on 04/11/2016

'''

class Function(object):
    '''
    classdocs
    '''
    def __init__(self):
        '''
        Constructor
        '''
    def function(self, x):
        self.y = (x**2.) - 3.0 
        return self.y

# funcoes criadas para realizar testes dos metodos implementados .		
		
    def function1(self, x):
        self.y = (7.*(x**3.))-(3*(x**2.))+(.2*x)-9.
        return self.y

    def function2(self, x):
        self.y = (-5. * (x ** 4.)) + (2. * (x ** 3.)) - (2. * (x ** 2.)) - 1.
        return self.y

    def function3(self, x):
        self.y = (x ** 7.) - (x ** 5.) + (2. * x) +47.
        return self.y

    def function4(self, x):
        self.y = (-1. * (x ** 2.)) - (16. * x) + 8.
        return self.y

    def function5(self, x):
        self.y = (x ** 8.) + (3.1415 * (x ** 6.)) - 14.
        return self.y

    def function6(self, x):
        self.y = (-23. * (x ** 2.)) - (-25. * x) + 0.25
        return self.y

    def function7(self, x):
        self.y = (0.5 * (x ** 4.)) + (11. * (x ** 3.)) + (2. * (x**2.))
        return self.y

    def function8(self, x):
        self.y = (117 * (x ** 2.)) - 257.
        return self.y

    def function9(self, x):
        self.y = (0.712 * (x ** 5.)) + (0.63 * (x ** 4.)) + (0.28 * x** 3.) - (0.058)
        return self.y

    def function10(self, x):
        self.y = (9. * (x ** 2.)) - (8. * x )
        return self.y

    def function11(self, x):
        self.y = (x ** 3) - (9.0 * x) + 3.

        return self.y

    def function12(self, x):
        self.y = (x ** 3) - (4 * (x**2)) + 2.

        return self.y

    def function13(self, x):
        self.y = (2*(x ** 3)) + (3 * (x ** 2)) - 2.
        return self.y